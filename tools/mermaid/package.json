{
  "name": "mermaid-trim-build",
  "version": "1.0.0",
  "private": true,
  "description": "Tree-shaken mermaid bundle (flowchart + sequenceDiagram only) for clwhiter.github.io",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node verify.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "mermaid": "11.15.0",
    "puppeteer": "25.7.0"
  }
}
